# Use pnpm as the package manager
# This file ensures consistent behavior across platforms

# Always install optional dependencies (needed for Rollup native bindings)
optional=true

# Strict peer dependencies handling
strict-peer-dependencies=false

# Enable hoisting for better compatibility
shamefully-hoist=true
