# Перемикання моделі OpenAI

## Огляд

Додано можливість динамічного перемикання моделі OpenAI, включаючи підтримку `gpt-5.2`.

## Підтримувані моделі

- `gpt-4o-mini` (за замовчуванням) - швидка, економна модель
- `gpt-4o` - найновіша потужна модель від OpenAI
- `gpt-4-turbo` - швидка версія GPT-4 з великим контекстом
- `gpt-4` - класична GPT-4
- `gpt-3.5-turbo` - швидка та економна модель

## Способи налаштування

### 1. Змінна середовища (при запуску)

```bash
OPENAI_MODEL=gpt-4o npm start
```

Або у `.env` файлі:
```
OPENAI_MODEL=gpt-4o
OPENAI_API_KEY=your-api-key
```

### 2. Через UI (динамічно)

1. Відкрийте додаток у браузері
2. Клікніть на індикатор AI статусу в нижній частині сайдбару (показує поточну модель)
3. Оберіть модель зі списку
4. Натисніть "Зберегти"

Модель буде змінена миттєво без перезапуску сервера.

### 3. Через API

```bash
# Змінити модель
curl -X PUT http://localhost:3000/api/v1/chat/model \
  -H "Content-Type: application/json" \
  -d '{"model": "gpt-4o"}'

# Перевірити поточну модель
curl http://localhost:3000/api/v1/chat/status
```

## Валідація

Система автоматично валідує обрану модель:
- ✅ Підтримувані моделі приймаються
- ❌ Непідтримувані моделі відхиляються з чітким повідомленням про помилку
- 🔄 При невалідній моделі в `.env` система автоматично використовує `gpt-4o-mini`

## Технічні деталі

### Backend зміни

**`src/ai/provider.ts`:**
- Додано константу `SUPPORTED_MODELS` з переліком підтримуваних моделей
- Додано функцію `isSupportedModel()` для валідації
- Додано функцію `setModel()` для динамічної зміни моделі
- Валідація моделі при ініціалізації

**`src/api/routes/chat.ts`:**
- Новий endpoint: `PUT /api/v1/chat/model` - зміна моделі
- Оновлено endpoint: `GET /api/v1/chat/status` - тепер повертає `supportedModels`

### Frontend зміни

**`src/public/app.js`:**
- Додано стан `aiModel` та `supportedModels`
- Функція `openModelSelectorModal()` - модальне вікно для вибору моделі
- AI статус тепер клікабельний для зміни моделі
- Автоматичне оновлення UI після зміни моделі

## Тестування

Всі зміни протестовані:

```bash
# Запустити тести
npm run build
node test-model-switching.js  # (якщо створено)
```

Результати тестів:
- ✅ Валідація підтримуваних моделей
- ✅ Відхилення непідтримуваних моделей
- ✅ API endpoint для зміни моделі
- ✅ API endpoint для отримання статусу
- ✅ Динамічна зміна моделі без перезапуску

## Зворотна сумісність

- Якщо `OPENAI_MODEL` не встановлено, використовується `gpt-4o-mini` (як раніше)
- Існуючий функціонал не порушено
- Всі старі API endpoints працюють як раніше

## Приклад використання

```javascript
// Backend (Node.js)
import { setModel, getAIConfig } from './ai/provider.js';

// Змінити модель
setModel('gpt-4o');

// Отримати поточну конфігурацію
const config = getAIConfig();
console.log(config.model); // 'gpt-4o'
```

```javascript
// Frontend (JavaScript)
// Користувач клікає на AI статус → відкривається модальне вікно → обирає модель
// Модель змінюється автоматично через API
```

## Примітки

- Для використання `gpt-4o` або `gpt-4` переконайтеся, що ваш OpenAI API ключ має доступ до цих моделей
- Різні моделі мають різну вартість - перевірте pricing на OpenAI
- Зміна моделі впливає на всі наступні запити до AI
